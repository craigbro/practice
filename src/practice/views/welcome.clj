(ns practice.views.welcome
  (:require [practice.views.common :as common]
            [noir.content.pages :as pages])
  (:use noir.core
        hiccup.core
        hiccup.page-helpers))

;;(defpage "/post/:name" {:keys :name}
  


